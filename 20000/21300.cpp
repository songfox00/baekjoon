#include<iostream>

using namespace std;

int main() {
	ios::sync_with_stdio(NULL);
	cin.tie(0);
	cout.tie(0);

	int a, b, c, d, e, f;
	cin >> a >> b >> c >> d >> e >> f;

	cout << (a + b + c + d + e + f) * 5;

	return 0;
}